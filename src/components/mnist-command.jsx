import React from 'react';
import ClearButton from '../containers/clear-button';
import ConfirmRetrainButton from '../containers/confirm-retrain-button';

const MnistCommand = () => (
  <div>
    <ClearButton>Clear</ClearButton>
    <ConfirmRetrainButton />
  </div>
);

export default MnistCommand;
